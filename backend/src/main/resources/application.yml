server:
  port: 8080

spring:
  application:
    name: cyan-backend
  datasource:
    url: jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:5432/postgres
    username: postgres.euiixfuhsnjldqvwdnea
    password: Trannguyendangtam11
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  cache:
    type: ${CYAN_CACHE_TYPE:simple}
    cache-names:
      - movies
      - movieRecommendations
  data:
    redis:
      host: ${CYAN_REDIS_HOST:localhost}
      port: ${CYAN_REDIS_PORT:6379}
      password: ${CYAN_REDIS_PASSWORD:}
      timeout: ${CYAN_REDIS_TIMEOUT:2000ms}

application:
  cors:
    allowed-origin-patterns: ${CYAN_ALLOWED_ORIGIN_PATTERNS:https://cyan-streaming.vercel.app,https://*.vercel.app,http://localhost:5173,http://127.0.0.1:5173}
  security:
    jwt:
      secret-key: ${CYAN_JWT_SECRET:cyan-streaming-super-secret-jwt-key-please-change}
      expiration: ${CYAN_JWT_EXPIRATION:86400000}
