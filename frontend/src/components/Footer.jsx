function Footer() {
  return (
    <footer className="border-t border-white/10 bg-slate-950/70">
      <div className="mx-auto flex max-w-7xl flex-col gap-3 px-4 py-8 text-sm text-slate-400 sm:px-6 lg:px-8 md:flex-row md:items-center md:justify-between">
        <p>Cyan blends a Netflix-style browsing experience with JWT auth, admin tooling, and YouTube-based streaming.</p>
        <p className="uppercase tracking-[0.3em] text-slate-500">Cyan Studio</p>
      </div>
    </footer>
  );
}

export default Footer;
